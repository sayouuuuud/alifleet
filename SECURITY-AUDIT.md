# تقرير الفحص الأمني والتقني الشامل — AliFleet

**التاريخ:** 6 سبتمبر 2026
**النطاق:** الفرونت إند (Next.js 16.2.6) + الباك إند (WordPress/WooCommerce عبر mu-plugin) + الربط بينهما
**المنهج:** قراءة كل نقاط الدخول من الإنترنت سطرًا بسطر، فحص المكتبات، فحص الأنواع، **وتأكيد كل ثغرة على الموقع الحي** قبل تسجيلها. مفيش بند هنا مبني على تخمين.

---

## 1) الملخص التنفيذي

| الخطورة | العدد | الحالة |
|---|---|---|
| 🔴 حرجة | **3** | لازم تتصلح قبل أي إعلانات |
| 🟠 عالية | **5** | خلال أيام |
| 🟡 متوسطة | **7** | خلال أسبوعين |
| 🔵 تحسينات | **6** | حسب الوقت |
| ✅ سليم | **14** | نقاط قوة موثّقة |

**الحكم العام:** البنية الأساسية (Headless: WordPress للبيانات + Next.js للعرض) **سليمة ومصممة كويس** — نظام الجلسات والـ JWT وربط الدفع كلها متعملة صح. المشاكل الحرجة كلها **إضافات ناقصة** (حماية من التخمين، حد للطلبات، CSP) مش عيوب في التصميم. يعني الإصلاح **إضافة طبقات** مش إعادة بناء.

**الخبر الكويس:** المكتبات صفر ثغرات، الأنواع صفر أخطاء، البناء ناجح، مفيش أسرار في الكود.

**الخبر اللي محتاج تحرك فوري:** اسم مستخدم الأدمن **مكشوف لأي حد** ومفيش حد لمحاولات الدخول — ده الطريق الأقصر لاختراق الموقع.

---

## 2) 🔴 الثغرات الحرجة

### C1 — اسم مستخدم الأدمن مكشوف + مفيش حد لمحاولات الدخول

**الدليل الحي:**
```
GET https://a-f.site/wp-json/wp/v2/users  →  200
[{"id":3,"name":"Ali Ahmad","slug":"ali", ...}]
```
وكمان من دومين المتجر: `alifleet.com/cms/wp-json/wp/v2/users → 200`

**ليه حرجة:** الاختراق بالتخمين محتاج حاجتين: اسم المستخدم والباسورد. الأول بقى معروف (`ali`). وبالفحص: **مفيش أي rate limiting** لا في Next.js (`loginAction`) ولا في ووردبريس (مفيش Wordfence ولا Limit Login Attempts في قائمة الإضافات). يعني برنامج بسيط يجرب آلاف الباسوردات من غير ما حد يوقفه.

**الأثر لو اتنفذت:** سيطرة كاملة على ووردبريس = كل الطلبات، بيانات العملاء، الأسعار، والقدرة على حقن كود في الموقع.

**الإصلاح:**
1. حجب `/wp-json/wp/v2/users` لغير المسجلين (فلتر `rest_endpoints` في الـ mu-plugin) — **أقدر أعمله**
2. تثبيت إضافة حد محاولات الدخول (Limit Login Attempts Reloaded أو Wordfence) — **محتاج إذنك** (إضافة جديدة)
3. تفعيل التحقق بخطوتين (2FA) لحساب الأدمن — **انت**
4. باسورد قوي فريد لحساب `ali` — **انت**

---

### C2 — مسار `/cms/` بروكسي مفتوح بالكامل لووردبريس

**الملف:** `app/cms/[[...path]]/route.ts` + `lib/checkout/proxy.ts`

**المشكلة:** المسار بيمرر **كل طرق HTTP** (GET/POST/PUT/PATCH/DELETE) لـ **أي مسار** في ووردبريس، مع **كل الكوكيز**، وبيرجّع كوكيز ووردبريس على دومين المتجر.

**الدليل الحي:**
```
alifleet.com/cms/wp-login.php       →  200  (صفحة دخول ووردبريس من دومين المتجر)
alifleet.com/cms/wp-json/           →  200  (فهرس كل مسارات REST)
alifleet.com/cms/wp-json/wp/v2/users →  200
```

**الأثر:** مساحة الهجوم اتضاعفت. أي ثغرة في ووردبريس (إضافة، ثيم) بقت متاحة من `alifleet.com` كمان، وكوكيز جلسة ووردبريس بتتحط على دومين المتجر. لو حد سرق جلسة أدمن، يقدر يشغّل wp-admin من خلال المتجر.

**الإصلاح (كود):** قائمة بيضاء للمسارات المسموح تمريرها فقط (`/checkout/*`، `/wc-ajax`، `/wp-admin/admin-ajax.php`، `/product/*`، `/my-account/*`) ومنع صريح لـ `wp-login.php`، `wp-admin/*`، `xmlrpc.php`، `wp-json/wp/v2/users`. **أقدر أعمله.**

---

### C3 — مسار الشات `/api/chat` مفتوح بدون حماية + بيكذب على العملاء

**الملف:** `app/api/chat/route.ts`

**3 مشاكل في ملف واحد:**

**أ) استنزاف مالي:** المسار بيستدعي نموذج ذكاء صناعي مدفوع (`AI_GATEWAY_API_KEY`) بدون تسجيل دخول، بدون حد للطلبات، بدون حد لطول الرسالة. أي حد يكتب سكربت يبعت آلاف الطلبات → **فاتورة بآلاف الدولارات** على حسابك.

**ب) معلومات غلط:** الـ system prompt بيقول:
```
- Genuine spare parts          ← انت بتبيع بديل
- importing from Japan, UAE    ← انت بتستورد من أوروبا وأمريكا وكندا
```
يعني الشات بوت **بيقول للعملاء كلام يناقض الموقع** اللي لسه صلحناه.

**ج) مفيش تحقق من الأصل:** أي موقع تاني يقدر يستدعي المسار ده.

**الإصلاح (كود):** حد للطلبات بالـ IP، حد لطول وعدد الرسائل، تحقق من `Origin`، وتصحيح الـ prompt بالحقائق الصح (بديل، أوروبا/أمريكا/كندا، الرينة، ساعات العمل، واتساب). **أقدر أعمله.**

---

## 3) 🟠 الثغرات العالية

### H1 — مفيش Content-Security-Policy ولا X-Frame-Options
**الدليل الحي:** الترويسات الموجودة: HSTS ✅، nosniff ✅، Referrer ✅، Permissions ✅. **الناقص:** CSP و X-Frame-Options.
**الأثر:** مفيش خط دفاع تاني ضد XSS (شوف H2)، والموقع ممكن يتحط في iframe لموقع خبيث (clickjacking).
**الإصلاح:** إضافتهم في `next.config.mjs` — محتاج عناية لأن فيه سكربتات inline (JSON-LD، البيكسل) وGSAP. **أقدر أعمله.**

### H2 — HTML الووردبريس بيتعرض بدون تنظيف
**الملفات:** `components/blog-article.tsx:105` و `components/policy-screen.tsx:306` — `dangerouslySetInnerHTML` على `content` جاي من ووردبريس **بدون أي sanitize** (اتأكدت: مفيش DOMPurify ولا sanitize-html في المشروع).
**الأثر:** أي XSS مخزّن في ووردبريس (إضافة مخترقة، محرر مخترق) **بيتنفذ على دومين المتجر** — يقدر يقرأ السلة ويعمل طلبات باسم الزائر. ومع H1 مفيش حاجة توقفه.
**الإصلاح:** تنظيف server-side بقائمة وسوم مسموحة. **أقدر أعمله.**

### H3 — صفر rate limiting في المشروع كله
**الدليل:** بحث شامل — مفيش أي مكتبة أو منطق حد للطلبات.
**المتأثر:** تسجيل الدخول، التسجيل، نسيت الباسورد، الشات، بروكسي الصور، بروكسي الدفع.
**الأثر:** تخمين باسوردات، إنشاء حسابات وهمية بالجملة، هجوم DoS على ووردبريس من خلال المتجر.

### H4 — البروكسي بيحمّل جسم الطلب كامل في الذاكرة بدون حد
**الملف:** `lib/checkout/proxy.ts` — `await request.arrayBuffer()` في 3 أماكن بدون حد للحجم، مع timeout 20 ثانية للسيرفر البعيد وبدون حد للطلبات المتزامنة.
**الأثر:** طلبات كبيرة متزامنة → استنزاف ذاكرة السيرفر، وتضخيم الحمل على ووردبريس.

### H5 — `.gitignore` مش بيحمي كل ملفات الأسرار
**الدليل:** بيتجاهل `.env*.local` بس. لو حد عمل `.env` أو `.env.production` هيترفع على GitHub بالأسرار.
**الإصلاح:** سطر واحد. **أقدر أعمله.**

---

## 4) 🟡 المتوسطة

| # | البند | الملف | الإصلاح |
|---|---|---|---|
| M1 | بروكسي الصور بيسمح بأي مسار على a-f.site (مش الصور بس)، وبدون تحقق من نوع المحتوى أو حد للحجم | `app/api/img/route.ts` | قصره على `/wp-content/uploads/` + أنواع صور + حد 10MB |
| M2 | `sanitizeRedirect` بتمنع `//` بس مش `/\evil.com` | `lib/auth/actions.ts:330` | تحقق أقوى |
| M3 | `frontendOrigin()` بتثق في هيدر `x-forwarded-host` لأن `SITE_ORIGIN` مش مضبوط | `lib/checkout/proxy.ts:92` | ضبط `SITE_ORIGIN=https://alifleet.com` في Coolify — **انت** |
| M4 | علم `secure` للكوكيز بيعتمد على `x-forwarded-proto` | `lib/auth/session.ts` | فرض `secure: true` في الإنتاج |
| M5 | مسار `revalidate` بيقبل السر في الرابط `?secret=` → بيتسجل في اللوجات | `app/api/revalidate/route.ts:37` | هيدر بس |
| M6 | `ignoreBuildErrors: true` + `build: next build \|\| next build --webpack` — بيخبوا أخطاء مستقبلية ويخلوا الإنتاج يختلف عن المحلي | `next.config.mjs`, `package.json` | شيلهم (حاليًا صفر أخطاء فآمن) |
| M7 | `/alifleet/v1/session` عنده `permission_callback => __return_true` — التحقق جوه الدالة (سليم) بس مربك لأي مطور جاي | mu-plugin:2119 | نقل التحقق لـ permission_callback |

---

## 5) 🔵 تحسينات ونظافة

- `package.json` اسمه `my-project` — بقايا قالب
- `/wp-json/` الجذر مكشوف — بيكشف كل المسارات المتاحة
- 36 منتج نافد + 3 بلا سعر (من الفحص السابق)
- `page_on_front = 0` بعد الأرشفة — صور الهوم راجعة للكود (من الفحص السابق)
- لوجات `[v0]` بتطبع 200 حرف من ردود ووردبريس — تنضيف
- اللغة الافتراضية `en` — بتتصلح في المرحلة 3

---

## 6) ✅ اللي متعمل صح (نقاط القوة)

دي مهمة تعرفها عشان ما تحسش إن كل حاجة مكسورة — العكس، الأساس متين:

| البند | الدليل |
|---|---|
| JWT في كوكيز `httpOnly` + `sameSite=lax`، عمره مش في localStorage | `lib/auth/session.ts` |
| مسار `/session` بيتحقق من الـ JWT عبر GraphQL `viewer` قبل ما يثق في أي user id | mu-plugin:1786 |
| كوكي `alifleet_customer_handoff` موقّع HMAC-SHA256 بملح ووردبريس — مش قابل للتزوير | mu-plugin:1818 |
| مسار `revalidate`: بيتقفل لو السر مش مضبوط، مقارنة ثابتة الزمن، POST فقط | `app/api/revalidate/route.ts` |
| عميل GraphQL server-only، بيرر توكن عند الحاجة بس، timeout 15s، بيتعامل مع ردود غير JSON | `lib/wp/client.ts` |
| معالج `alifleet-cart`: `absint`، تحقق `is_purchasable`/`is_in_stock`، قصر الكمية على المخزون، `wp_safe_redirect` | mu-plugin:59-103 |
| CORS: قائمة أصول محددة + credentials — مش `*` | mu-plugin:379 |
| بروكسي الصور بيقبل دومين ووردبريس بس | `app/api/img/route.ts:31` |
| بوابة الدفع بتمنع عرض سلة ووردبريس قديمة، وتسجيل الخروج بيمسح كوكيز Woo (خصوصية) | `lib/checkout/gate.ts`, `actions.ts:168` |
| `xmlrpc.php` محجوب | 405 حي |
| HSTS + nosniff + Referrer-Policy + Permissions-Policy موجودين | حي |
| `npm audit`: **0** ثغرات في كل المستويات | فحص |
| `tsc --noEmit`: **0** أخطاء أنواع | فحص |
| الواجهة العامة لووردبريس محجوبة (headless redirect) | 302 → wp-login حي |
| مفيش أسرار مكتوبة في الكود | بحث شامل |

---

## 7) حكم البنية الأساسية

**التصميم:** Headless CMS — ووردبريس مخزن بيانات + Next.js واجهة. ده نمط معتمد عالميًا ومناسب لعملك.

**نقاط القوة الهيكلية:**
- فصل واضح: البيانات في ووردبريس، العرض في Next
- الفرونت شغال حتى لو ووردبريس وقع (fallback layer)
- الدفع بيعدي من بروكسي محكم مع بوابة حالة
- الكاش بمستويات (10 دقايق بيانات، ساعة خريطة)

**نقطتا الضعف الهيكليتان:**
1. بروكسي `/cms` مفتوح (C2) — المفروض يكون بقائمة بيضاء من أول يوم
2. مفيش طبقة حماية عامة (rate limit + CSP) — دي طبقة واحدة بتحل C1/C3/H1/H3 مع بعض

**الحكم:** الأساس **كامل وصحيح**. مفيش حاجة محتاجة إعادة بناء.

---

## 8) نتائج QA

| الفحص | النتيجة |
|---|---|
| `npm run build` (Turbopack) | ✅ نجح |
| `tsc --noEmit` | ✅ 0 أخطاء |
| `npm audit` | ✅ 0 ثغرات |
| robots.txt / sitemap.xml | ✅ حي — 152 رابط |
| مسار الشراء كامل (متصفح حقيقي) | ✅ من المنتجات لصفحة الدفع |
| Open Graph + Schema | ✅ حي |
| سرعة الاستجابة | ✅ 0.36s TTFB، صور AVIF 20KB |

---

## 9) اقتراحات النمو

### الأولوية القصوى (أثر مباشر على المبيعات)
1. **مسارات اللغات + hreflang** (المرحلة 3) — دلوقتي العربي والإنجليزي **غير مرئيين** لجوجل. ده أكبر رافعة SEO موجودة.
2. **الدفع بالبطاقة** — حاليًا كاش عند الاستلام بس. معظم المتسوقين أونلاين بيتوقعوا بطاقة.
3. **مناطق شحن** — حاليًا استلام من المحل بس. توصيل للشمال/كل إسرائيل بيوسّع السوق.
4. **زر واتساب عائم** (طلبته) — بتتبع UTM عشان تعرف مصدر كل محادثة.

### SEO محلي
5. **Google Business Profile** للرينة — ظهور في الخرائط، أهم حاجة لعميل بيدوّر على "قطع غيار شاحنات قريب مني"
6. توسيع Schema: `geo` + `areaServed` + `priceRange` + FAQ على صفحة السيارات
7. **قسم آراء العملاء** من تيك توك/فيسبوك مع `Review` schema — **حقيقية فقط**

### المحتوى
8. **مدونة بأرقام القطع**: "مصباح DAF XF 106 رقم القطعة..." — كده الميكانيكي بيدوّر
9. صفحة لكل ماركة (DAF, MAN, Volvo...) — كلمات بحث عالية القيمة

### التشغيل
10. **مراقبة توقف** (UptimeRobot) — الموقع وقع 503 مرتين خلال الأسبوع ده من غير ما حد يعرف
11. **تنبيه أخطاء** (Sentry) — عشان تعرف لو الدفع فشل لعميل
12. **بيئة تجريبية** في Coolify (branch preview) — تختبر قبل `main`
13. **نسخ احتياطي خارجي** — UpdraftPlus منصّب، لازم تتأكد من الجدولة والتخزين بره السيرفر
14. GA4 + أحداث تحويل (add_to_cart, begin_checkout, purchase) على البيكسل كمان

---

## 10) خطة التنفيذ بالأولوية

### 🔴 المرحلة الأمنية الفورية — أقدر أعملها كلها في الكود/mu-plugin

| # | الإصلاح | يحل |
|---|---|---|
| 1 | حجب `/wp-json/wp/v2/users` في الـ mu-plugin | C1 |
| 2 | قائمة بيضاء لبروكسي `/cms` | C2 |
| 3 | rate limit + حدود + تصحيح prompt للشات | C3, H3 |
| 4 | CSP + X-Frame-Options | H1 |
| 5 | تنظيف HTML ووردبريس | H2 |
| 6 | حد حجم جسم الطلب في البروكسي | H4 |
| 7 | `.gitignore` للأسرار | H5 |
| 8 | تقوية `sanitizeRedirect` + قصر بروكسي الصور | M1, M2 |

### 🟠 محتاجة منك

| # | الإجراء | يحل |
|---|---|---|
| 9 | إذن تثبيت إضافة حد محاولات الدخول (Limit Login Attempts Reloaded) | C1 |
| 10 | تفعيل 2FA + باسورد قوي لحساب الأدمن | C1 |
| 11 | ضبط `SITE_ORIGIN=https://alifleet.com` و `WORDPRESS_REVALIDATE_SECRET` في Coolify | M3 |
| 12 | قرار: الدفع بالبطاقة؟ مناطق الشحن؟ | نمو |

---

## ملاحظة أخيرة

كل ثغرة في التقرير ده **اتأكدت على الموقع الحي** أو بقراءة الكود سطرًا بسطر — مفيش تخمين. والأساس اللي المطور بناه **كويس فعلًا**؛ اللي ناقص طبقة حماية بتتضاف من غير ما تلمس التصميم.

لو وافقت، أبدأ في المرحلة الأمنية الفورية (البنود 1–8) على فرع منفصل، وأختبر كل بند، وأسلّمك تقرير تنفيذ.
